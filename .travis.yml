language: c

compiler:
  - gcc
  - clang

dist: trusty
sudo: false

addons:
  apt:
    packages:
      - astyle
      - libcunit1-dev
      - libaio-dev
      - libssl-dev
      - libnuma-dev
      - uuid-dev

before_script:
  - git submodule update --init
  - export MAKEFLAGS="-j$(nproc)"

script:
  - ./configure --enable-werror --without-isal --enable-debug
  - make
  - ./test/unit/unittest.sh
